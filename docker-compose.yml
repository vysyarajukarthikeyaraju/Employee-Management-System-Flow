
version: '3.8'
services:
 app:
  build: .
  ports:
   - "8080:8080"
  depends_on:
   - db
   - rabbitmq

 db:
  image: mysql:8.0
  environment:
   MYSQL_ROOT_PASSWORD: root
   MYSQL_DATABASE: employeedb

 rabbitmq:
  image: rabbitmq:3-management
  ports:
   - "5672:5672"
   - "15672:15672"
